
export default [
  config => {
    // console.log('request', config);
    return config
  },
  error => {
    // logger.errLog('=======Error request from frontend=======', JSON.stringify(error, null, 2))
  }
]
